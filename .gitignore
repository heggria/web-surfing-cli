# macOS
.DS_Store

# Node / Bun
node_modules/
dist/*
!dist/cli.mjs
*.tgz
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-store/

# Editor
.vscode/
.idea/
*.swp

# Local state / secrets — defense in depth (these live under ~/.config and ~/.local
# normally, but if someone cp them into the repo we don't want to commit them)
keys.toml
keys.ini
*.key
.env
.env.*
!.env.example
